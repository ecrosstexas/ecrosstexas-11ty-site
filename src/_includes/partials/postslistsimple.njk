{% set postAuthor = post.data.author | default(meta.author, true) %}

<section>
  {% for post in postslist | reverse %}
    <article{% if post.url == url %} data-current="current item"{% endif %} class="mb-8">
      <div>
        <a href="{{ post.url | url }}">
          {{ post.data.title }}
        </a>
      </div>
      <div class="text-sm">
        {{ post.data.excerpt }}
      </div>
      <div class="text-sm italic">

      </div>
    </article>
  {% endfor %}
</section>